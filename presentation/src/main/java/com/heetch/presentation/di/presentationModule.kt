package com.heetch.presentation.di

import org.koin.core.module.Module
import org.koin.dsl.module

val presentationModule: List<Module> = mutableListOf<Module>().apply {
    module {
        //TODO: add view model modules
    }
}